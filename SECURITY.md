# Security

## 1. 密钥管理

- Qwen / 百炼 API Key 只能放在后端环境变量或部署平台 Secrets。
- 不允许提交 `.env`、数据库密码、Token、私钥。
- 不允许在前端代码、构建产物、截图、日志、文档中出现密钥。
- `.env.example` 只能使用占位值。

## 2. 模型调用安全

- 所有模型调用统一经过后端 Qwen Client。
- Prompt 和模型输出需要记录版本和模型名。
- 模型输出必须经过 JSON Schema 校验。
- 无来源的模型结论不能作为最终事实展示。
- 用户输入需要限制长度，避免异常 Prompt 或成本失控。

## 3. 公网 Demo 安全

- 限制主案例和调用频率。
- 对模型调用、导出和反馈接口设置基础限流。
- 外部服务失败时使用真实运行缓存。
- 缓存结果必须标注 `cached: true` 或页面提示。

## 4. 数据与日志

- 日志不得记录完整 API Key、数据库连接串或过长模型响应。
- 外部数据源结果需要记录来源 URL、查询参数、获取时间。
- 反馈内容按普通文本处理，不执行、不拼接为系统指令。
- 导出文件不包含内部调试信息。

## 5. GitHub 安全

- 所有改动通过 PR。
- Secret scanning、依赖漏洞检查、CodeQL 保持启用。
- 部署密钥暂不配置，除非后续有明确部署需求。
- 成员加入后按最小权限原则分配。

## 6. 发现泄露时

1. 立即撤销对应密钥。
2. 从部署平台和本地环境重新配置新密钥。
3. 检查 Git 历史、Actions 日志、截图和文档。
4. 在 `docs/quality/RISK_REGISTER.md` 记录原因和修复措施。
