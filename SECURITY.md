# Security

## API Key 管理

- Qwen / 百炼 API Key 只能放在后端环境变量中。
- 不允许提交 `.env` 文件。
- 不允许在前端代码中写任何密钥。
- 不允许把密钥写入截图、日志或文档。

## 公网 Demo

- 公网 Demo 必须限制调用频率。
- 需要提供缓存兜底，避免外部 API 异常导致演示失败。
- 自定义输入可以限制范围，主案例必须稳定可用。

## 数据与日志

- 日志中不要记录完整 API Key。
- 外部数据源结果需要记录来源和查询时间。
- 缓存结果必须来自真实运行。

