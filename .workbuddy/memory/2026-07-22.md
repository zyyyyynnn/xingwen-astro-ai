# 2026-07-22

## A-14 前端 Domain、Fixture Adapter 与 Guided Tour FSM (Issue #87)

- 分支 `feat/a-14-domain-fixture-fsm`，PR #115 (Draft)，Issue #87 标记 in-progress。
- **packages/domain**: 纯 TS 领域模型 (ResearchProject, ResearchContractDraft, ResearchContract, ResearchRun, RunEvent, ArtifactVersion, Evidence, ProvenanceState) + 不变量校验。框架无关，由 check:architecture 强制。
- **packages/contracts**: vendored B-15 JSON Schema + ajv 运行时校验 + json-schema-to-typescript 生成 DTO 类型。sync 脚本 `scripts/sync_v2_contracts.mjs`。新增依赖 ajv@8.20.0, ajv-formats@3.0.1 (contracts), json-schema-to-typescript@15.0.4 (root devDep)。
- **packages/data-access**: Repository Port (5 个仓储, 读写+订阅) + Fixture Adapter (校验 DTO, 强制 demo_replay/fixture 语义, snake_case→camelCase 映射)。Fixture 场景: exoplanet-host-star。
- **packages/workspace-core**: Guided Tour FSM (8 阶段: signal→question→acquire→resolve→reason→map→inspect→continue)，支持启动/暂停/恢复/跳过/跳转/模式选择/重置。
- 68 单元测试 (contracts 23 + data-access 21 + workspace-core 24)。全部门禁 PASS (pnpm check + check_foundation.py)。
- 关键发现: pnpm 在本环境通过 `node E:/DevEnv/NodeJS/cache/_npx/.../pnpm.cjs` 运行 (corepack shim 损坏); node 24.15.0 可用 (engines 要求 24.18.0 但 engine-strict 未拦截 pnpm)。
- 包 tsconfig 需 `exclude: ["src/**/*.test.ts"]` 以避免 tsc 对测试文件中 node: API 的类型检查 (与 design-tokens 一致)。
