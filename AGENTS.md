# AGENTS

本文件是本仓库的 Agent 执行协议。协作与合并流程见 [CONTRIBUTING.md](CONTRIBUTING.md)，文档地图与唯一事实来源见 [docs/README.md](docs/README.md)。

## 1. 默认读取顺序

1. **`AGENTS.md`**：确定执行协议与核心约束。
2. **当前 Issue 或用户直接授权**：明确任务目标、范围与验收标准。
3. **`docs/README.md`**：定位权威规范路径。
4. **1–3 份直接相关 Authority 文档**：仅读取与当前任务直接相关的规范正文。

**上下文控制规则：**
- 不默认读取 `docs/references/` 下的参考资料。
- 不默认递归读取所有模块级 Markdown。
- 不默认读取历史 Commit SHA、关闭的 PR 或历史方案。
- 仅在任务明确需要时才按需扩展上下文。

## 2. 真实性与行为边界

- 不编造事实、代码、接口、文件内容、历史记录或验证结果。
- 先定位唯一事实来源，不绕过 Domain、Repository、Workflow 与 Data Versioning 边界。
- 改变公共接口、数据契约、工作流、版本、安全或 UI 规则时，必须同步更新对应的唯一 Authority。
- 不做请求之外的功能，不夹带无关格式化、重构或依赖调整。

## 3. 工作区与 Git 安全

- 修改前检查工作区状态与相关 diff，保留已有有效变更。
- 未经明确要求，不执行 destructive Git 操作，不直接提交 `main`、推送、变基、合并或切换分支。
- 不输出、提交或传播密钥、凭据与隐私数据。
- 涉及生产配置或不可逆迁移时，先确认影响范围与授权边界。

## 4. 验证与报告

- 优先使用实际运行、针对性测试、项目构建与静态检查验证结果。
- 不跳过已有且直接相关的测试；测试失败时须区分新引入缺陷与既有缺陷。
- 无法验证时显式说明“未验证”、原因与建议验证命令。
- 只报告实际执行并观察到的真实结果。
